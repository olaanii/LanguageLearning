class Env {
  // Flag indicating if we are in development mode
  static const bool isDevelopment = true;

  // Placeholder for any other environment-specific variables
  // static const String baseUrl = isDevelopment ? 'http://localhost:8080' : 'https://api.production.com';
}
